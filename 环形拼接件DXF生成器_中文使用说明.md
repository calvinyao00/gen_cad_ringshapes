# 环形拼接件 DXF 生成器使用说明

## 一、程序用途

本程序用于生成激光切割用的环形拼接件 DXF 文件。

输入环形尺寸和拼接参数后，程序默认会生成一个独立的环形扇区 DXF 模板文件。零件数量只用于计算单片扇区角度；在激光切割软件中复制和旋转这个模板即可排版完整圆环。

程序支持：

- 自定义内径和外径
- 自定义零件数量
- 根据环宽自动计算缺口尺寸
- 手动输入缺口尺寸
- 梯形缺口或矩形缺口
- 自定义激光切割间隙
- 输出单个零件模板文件
- 输出完整环形布局文件（可选）
- DXF 预览
- 自动生成中文文件名

---

## 二、Windows 和 macOS 安装与启动

### 1. 安装 Python

从 Python 官网下载并安装 Python 3：

<https://www.python.org/downloads/windows/>

安装时请勾选：

`Add python.exe to PATH`

### 2. 启动程序

将以下文件放在同一个文件夹中：

- `ring_generator_gui.py`
- `generate_ring.py`
- `运行环形生成器.bat`

Windows 用户双击：

`运行环形生成器.bat`

程序会打开中文 Tkinter 桌面窗口。

macOS 用户在程序文件夹中打开终端，运行：

```bash
python3 ring_generator_gui.py
```

程序会打开中文 Tkinter 桌面窗口。

关闭桌面窗口即可退出程序，不会保留浏览器或本地服务器进程。

本程序使用 Python 标准库，不需要安装 pip 第三方包。Windows EXE 会将 Tkinter 一起打包。

---

## 三、参数说明

### 1. 内径

圆环内侧的直径，单位为毫米。

例如输入 `203`，表示内径为 203 mm，内半径为 101.5 mm。

### 2. 外径

圆环外侧的直径，单位为毫米。

外径必须大于内径。

### 3. 板厚

这里输入的是单侧板厚，单位为毫米。程序会按两侧总补偿计算切割尺寸：

```text
切割内径 = 输入内径 + 板厚 × 2
切割外径 = 输入外径 - 板厚 × 2
```

例如输入板厚 `3` mm，则切割内径增加 `6` mm，切割外径减少 `6` mm。文件名中的“+6补”表示这两侧合计补偿 6 mm。

### 4. 零件数量

组成完整圆环的扇区数量。

例如：

- 输入 `4`：生成 4 个 90° 扇区
- 输入 `6`：生成 6 个 60° 扇区
- 输入 `8`：生成 8 个 45° 扇区

零件数量至少为 2。

### 5. 缺口形状

程序提供两种形状：

#### 梯形

适合使用原始 DXF 中的斜面式拼接结构。默认形状就是梯形。

#### 矩形

使用方形台阶和直角肩部，轮廓更规则，便于需要直角加工效果的情况。

### 6. 根据环宽自动设计缺口

勾选此选项后，程序根据环的径向宽度自动计算缺口尺寸。

径向环宽计算方式为：

```text
环宽 = （外径 - 内径） / 2
```

自动设计会参考原始 DXF 的比例，并在缺口区域保留连续连接宽度。改变内径或外径后，缺口尺寸会自动更新。

当前自动缺口比例为径向环宽的 25%，适合需要保留较多焊接连接材料的情况。

### 7. 缺口尺寸

取消“根据环宽自动设计缺口”后，可以手动输入缺口尺寸，单位为毫米。

缺口不能过大。如果缺口尺寸相对于环宽过大，程序会提示错误。此时应减小缺口尺寸，或者增大外径与内径之间的差值。

### 8. 激光间隙

激光间隙是相邻拼接件之间预留的加工和装配间隙，单位为毫米。

间隙受到以下因素影响：

- 激光切割设备的实际切缝宽度
- 钢板厚度
- 材料热变形
- 是否需要喷漆或表面处理
- 希望装配紧密还是容易拆装

钢板较厚、切缝较大或需要更松的装配时，可以适当增大该数值。建议先用废料试切，再确定最终间隙。

### 9. 输出方式

#### 输出单个零件模板文件（推荐）

这是默认方式。程序只生成 1 个 DXF 文件，文件中只包含 1 个扇区。零件数量只决定扇区角度；例如输入 6，生成的就是一个 60° 单片模板。你可以在激光排版软件中复制这个文件 6 次，再旋转和移动到钢板上。

文件名示例：

```text
203x262+6补.dxf
```

文件中只包含一个扇区，排版时由你复制、旋转和移动。零件数量不会生成重复文件。

#### 输出完整环形布局文件

此方式生成一个包含所有扇区的 DXF 文件，扇区已经按照圆心排列成完整圆环。此方式适合检查拼接效果，不适合作为钢板排版的默认文件。

---

## 四、输出文件名

程序只根据内径、外径和板厚双边补偿生成文件名，格式为：

```text
<内径>x<外径>+<两侧总补偿>补.dxf
```

例如：

```text
203x262+6补.dxf
```

文件名中各部分含义：

- `203`：内径 203 mm
- `262`：外径 262 mm
- `+6补`：输入板厚为 3 mm，内外径两侧总补偿为 6 mm

缺口尺寸、激光间隙、缺口形状和零件数量不写入文件名。

梯形和矩形缺口使用相同的文件名规则，因为文件名只保留内径、外径和板厚双边补偿信息：

```text
203x262+6补.dxf
```

如果选择“输出完整环形布局文件”，文件名末尾会改为：

```text
203x262+6补_环形布局.dxf
```

---

## 五、生成 DXF 的步骤

1. 启动 `运行环形生成器.bat`。
2. 输入内径和外径。
3. 输入零件数量。
4. 选择“梯形”或“矩形”缺口。
5. 根据需要调整激光间隙。
6. 保持“根据环宽自动设计缺口”勾选，或取消勾选后手动输入缺口尺寸。
7. 在“输出方式”中选择“输出单个零件文件”。
8. 查看右侧预览和自动文件名。
9. 选择输出文件夹。
10. 点击“生成 DXF”。
11. 将生成的单个零件模板导入激光切割排版软件，复制、旋转并放置到钢板上。
12. 使用 CAD 软件打开 DXF，确认尺寸、缺口方向和拼接关系后再进行正式切割。

---

## 六、推荐的试切流程

正式切割前建议：

1. 使用相同钢板厚度和相同激光参数进行小尺寸试切。
2. 先切割一个或两个拼接件。
3. 检查缺口是否能顺利插入。
4. 如果太紧，增大激光间隙。
5. 如果太松，减小激光间隙。
6. 确认拼接后圆环内径和外径满足要求。
7. 最后再批量切割。

---

## 七、常见问题

### 双击 BAT 文件后没有窗口

请确认：

- 已安装 Python 3
- 安装时已勾选 `Add python.exe to PATH`
- `ring_generator_gui.py` 和 `generate_ring.py` 与 BAT 文件在同一文件夹

也可以打开命令提示符，在程序文件夹中运行：

```text
py -3.8 ring_generator_gui.py
```

### 提示外径必须大于内径

请确认外径数值大于内径数值。

### 提示缺口尺寸过大

请减小手动缺口尺寸，或者重新启用自动缺口设计。

### DXF 在 CAD 中看起来不完整

请确认 CAD 软件使用毫米单位，并检查图层是否显示。程序输出的是由 ARC 和 LINE 实体组成的 ASCII DXF 文件。

---

## 八、设计安全说明

自动缺口尺寸是根据原始 DXF 比例和几何连接宽度制定的加工经验规则，不是经过认证的结构应力计算。

如果圆环用于承重、旋转、高温、冲击或人员安全相关设备，请由合格工程师根据以下因素进行校核：

- 钢材牌号和屈服强度
- 板材厚度
- 环的实际受力
- 缺口处的应力集中
- 焊接或装配方式
- 疲劳寿命
- 激光切割热影响区
